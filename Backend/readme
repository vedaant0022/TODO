# To install backend in any project 

# npm init
# npm install express

# Make index.js in directory

# Paste it in index.js

# <!-- const express = require('express')
# const app = express()
# const port = 3000

# app.get('/', (req, res) => {
#   res.send('Hello World!')
# })

# app.listen(port, () => {
#   console.log(`Working ${port}`)
# }) -->

# Cors middleware
# npm i cors

# <!-- dotenv files to store confidential files -->
# npm i dotenv
# npm install dotenv --save
# create .env file in root directory
# Define port in .env file
# paste it index.js
# require('dotenv').config()

# <!-- Can you digital ocean for host local host alternative is ngrok  -->

# For data modeling mongoose schema is used 

# npm i mongoose

# Create a models folder in directory 

# <!-- //paste in model -->

# import mongoose from "mongoose"

# const userSchema = new mongoose.Schema(
#     {
#         username:{
#             type:String,
#             required: true,
#             unique: true,
#             lowercase: true
#         }, {timestamps: true}
#     }
# )

# export const User = mongoose.model("User", userSchema)




